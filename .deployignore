# Exclusions propres au plugin dahuavtobe, ajoutées à celles de deploy-plugin.sh.
# Un motif rsync par ligne ; les lignes vides et celles commençant par # sont
# ignorées par --exclude-from.

# Le jeu de contrôles ne sert qu'au développement : il interroge le coeur par
# réflexion depuis le dépôt et n'a rien à faire dans la racine web.
# (data/snapshots/ n'a pas besoin d'y figurer : l'outil l'exclut déjà.)
tests/
